import { Link } from "gatsby"
import { styled } from "styled-components"

export const WritingsBackLink = styled(Link)`
	text-decoration: none;
	margin: 16px;
	color: gray;
	code {
		font-size: 0.8rem;
	}
	font-size: 0.8rem;
`

export const Info = styled.div`
	margin-left: 8px;
	display: flex;
	flex-direction: row;
	// font-weight: 700;
`

// export const WritingsHeader = styled.div`
// 	display: flex;
// 	flex-direction: column;
// 	margin-bottom: 48px;
// `
