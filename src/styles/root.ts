import { css } from "styled-components"

const root = css`
	:root {
		// Dimensions
		--heroheader-height: 480px;
		--navigation-bar-height: 48px;
		--credits-height: 48px;
		--footer-height: 320px;
		--developed-height: 48px;
		--page-width: 960px;
		--gutter: 16px;

		// Interaction
		--disable-scroll: visible;
	}
`

export default root
